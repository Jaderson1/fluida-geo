// OpenFreeMap "dark": their own docs mark it as an incomplete style, and
// several layers (buildings, major roads) don't render below zoom ~11-12,
// which is why the map looked near-solid black at the previous zoom (10.6).
export const BASEMAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/liberty';

export const ATTRACTIONS_SOURCE_ID = 'attractions';
export const ATTRACTIONS_LAYER_ID = 'attractions-points';

export const INITIAL_ZOOM = 10.6;